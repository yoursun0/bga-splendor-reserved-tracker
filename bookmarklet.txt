// Bookmarklet: drag a new bookmark and set URL to the code below (single line).
javascript:(function(){/* Minimal overlay + scan for "reserved" nodes. Adapt selectors. */const S=['.reserve','.card.reserved','[data-reserved]'];function extract(el){return{html:el.outerHTML,text:(el.innerText||'').trim().slice(0,200)}};function scan(){const found=[];S.forEach(sel=>{document.querySelectorAll(sel).forEach(e=>found.push(extract(e)))});return found;}function show(cards){let o=document.getElementById('bga-reserved-overlay');if(!o){o=document.createElement('div');o.id='bga-reserved-overlay';o.style.cssText='position:fixed;right:10px;top:10px;z-index:999999;background:#fff;border:1px solid #666;padding:8px;max-width:320px;max-height:70vh;overflow:auto;font-family:Arial;font-size:12px';document.body.appendChild(o);}o.innerHTML='<b>Reserved Cards</b><hr/>'+ (cards.length?cards.map(c=>'<div style="margin-bottom:6px"><div style="font-weight:600">'+(c.text||'card')+'</div><div style="font-size:11px">'+(c.html.slice(0,200))+'</div></div>').join(''):'(none detected)');}show(scan());})(); 